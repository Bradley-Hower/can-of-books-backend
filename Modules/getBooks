'use strict';

const Book = require('../Model/book');

function getBooks(req, res, next){
  let queryObject = {};

  if(req.query.title){
    query.Object = {name: req.query.title}
  }

  Book.find({})
    .then(data => res.status(200).send(data))
    .catch(err => next(err));
}

module.exports = getBooks;
